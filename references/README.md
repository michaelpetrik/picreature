# Style References

Drop your fixed brand portrait references into this folder before running real jobs.

Expected filenames:

- `style-reference-1.jpg`
- `style-reference-2.jpg`
- `style-reference-3.jpg`

The app tolerates missing files and falls back to text-only style rules, but true consistency is better once these references exist.
